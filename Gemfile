source "http://rubygems.org"

gem "rack"
gem "rake"
gem "puma"

group :deployment do
  # Warbler is used to package the application up as a java archive
  gem "warbler", :platform => :jruby
end

gem "sinatra"